ptor.findElement(protractor.By.x())
ptor.findElements(protractor.By.x())

// possible locators (provided by Webdriver)
protractor.By.className
protractor.By.css
protractor.By.id
protractor.By.linkText
protractor.By.partialLinkText
protractor.By.name
protractor.By.tagName
protractor.By.xpath

// additional locators (provided by protractor)
protractor.By.binding({status}})
protractor.By.select('user')
protractor.By.selectedOption("red")
protractor.By.input("user")
protractor.By.repeater("cat in pets")
protractor.By.repeater("cat in pets").row(1)
protractor.By.repeater("cat in pets").row(1).column("{{cat.name}}")

protractor.waitForAngular ?

// methods on returned locators
clear()  if this is a text entry element, this will clear the value
click()  click this element
getAttributeName(name)    get the value of a given attribute name
getCssValue(propertyName)   get value of specific css property
getLocation()    where on page is top left handed corner of rendered element
getSize();       what is the width and height of the element
getTagName()     get the tag name of the element
getText()   get the visible text of the element incl sub elements
isDisplayed()
isEnabled()
isSelected()
sendKeys(keysToSend)

